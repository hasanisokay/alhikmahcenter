'use client'

import { useState } from "react";

const Page = () => {
  const [loading, setLoading] = useState(false);
  
    return (
        <div>
          appointments  
        </div>
    );
};

export default Page;